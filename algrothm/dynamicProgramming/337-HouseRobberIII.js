const { test } = require('../tree/test')

/**
 * Definition for a binary tree node.
 * function TreeNode(val) {
 *     this.val = val;
 *     this.left = this.right = null;
 * }
 */
/**
 * @param {TreeNode} root
 * @return {number}
 */
var rob = function(root) {
  const memo = {}
};

const _rob = (root, memo) => {
}
console.log(test(rob, [3,2,3,null,3,null,1]))
console.log(test(rob, [3,4,5,1,3,null,1]))
